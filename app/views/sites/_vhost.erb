<VirtualHost *:80>
  ServerName <%= site.domain %>
  ServerAlias <%= [site.alias, site.sub_domain].join(' ') %>
  
  DocumentRoot <%= File.join site.install_path, 'wordpress' %>
  
  <Directory />
    Options FollowSymLinks
    AllowOverride All
    Order Deny,Allow
  </Directory>
</VirtualHost>