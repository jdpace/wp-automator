<VirtualHost *:80>
  ServerName <%= site.sub_domain %>
  ServerAlias <%= [site.domain, site.alias].join(' ') %>
  
  DocumentRoot <%= File.join site.install_path, 'wordpress' %>
  
  <Directory />
    Options FollowSymLinks
    AllowOverride None
    Order Deny,Allow
  </Directory>
</VirtualHost>